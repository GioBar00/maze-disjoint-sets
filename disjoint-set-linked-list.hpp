#include <vector>
#include "disjoint-set.hpp"

//TODO: disjoint set template using linked list
template <typename T>
struct ListDisjointSet : DisjointSet
{

    DisjointSet(int n) override
    {

    }

    T find(T x) override
    {
        
    }

    void unite(T x, T y) override
    {
        
    }
};
